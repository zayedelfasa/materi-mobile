import 'package:flutter/material.dart';

class HomePageCarousel extends StatelessWidget {
  const HomePageCarousel({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // untuk slide carousel
    );
  }
}