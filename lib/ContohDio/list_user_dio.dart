import 'package:flutter/material.dart';

class ListUserDio extends StatelessWidget {
  const ListUserDio({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Contoh User Dio"),
      ),
      body: Container(),
    );
  }
}